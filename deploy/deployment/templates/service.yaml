apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-sidecar.fullname" . }}
  labels:
    {{- include "helm-sidecar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-sidecar.selectorLabels" . | nindent 4 }}
